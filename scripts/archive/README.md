# Script Archive

This directory holds legacy or highly specialized wrappers that are no longer part of the active operator surface.

Rules:
1. keep active operator entrypoints in `scripts/`
2. move thin aliases, one-off local wrappers, and superseded variants here
3. do not point active docs at archived wrappers
